export function DisplayAdSlot(_props: {
  slot: string;
  label?: string;
  compact?: boolean;
  format?: "auto" | "fluid";
  layoutKey?: string;
}) {
  // Advertising is disabled in the standalone demonstration.
  return null;
}
